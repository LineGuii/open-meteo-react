import { OpenMeteoApi as OpenMeteoImport } from './open-meteo';
export const OpenMeteoApi = new OpenMeteoImport();

import { GeocodeMapsApi as GeocodeMapsImport } from './geocode-maps';
export const GeocodeMapsApi = new GeocodeMapsImport();
